{
  "name": "thoughtpad-fileWriter",
  "version": "0.0.1",
  "description": "Thoughtpad module for writing files to filesystem",
  "engines": {
    "node": "0.11.14",
    "npm": "2.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/thoughtpad/thoughtpad-fileWriter.git"
  },
  "dependencies": {  
    "co-fs": "~1.2.0",
    "co-fs-plus": "~0.3.1"
  },
  "devDependencies": {
    "should": "~3.3.2",
    "mocha": "~1.21.4",
    "coveralls": "~2.11.2",
    "istanbul-harmony": "~0.3.0",
    "co": "~3.1.0"
  },
  "main": "./src/main.js",
  "scripts": {
    "test": "node --harmony ./node_modules/.bin/istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  }
}
